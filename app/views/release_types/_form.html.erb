<%= form_with(model: release_type) do |form| %>
  <% if release_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(release_type.errors.count, "error") %> prohibited this release_type from being saved:</h2>

      <ul>
        <% release_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
